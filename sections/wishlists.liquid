<div class="page-width">
  <h2>wishlists</h2>
  <div class="wishlist-row">
  </div> 
</div>
<script>
   $(document).ready(function(){
     {% for product in collections.all.products %}
       var wishid = localStorage.getItem("wishlist_{{ productid }}");
       console.log(wishid);
       {% endfor %}
   });
</script>
{% schema %}
  {
    "name": "Wishlists",
    "settings": []
  }
{% endschema %}